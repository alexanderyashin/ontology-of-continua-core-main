# OC Continuous Universal Metamodel Completion Order 028

## Governing Rule

028 is the continuation layer after the 027 OC-U repair. It is not allowed to
stop at the statement "027 proves the formal metamodel core." The task is to
bind that core back to the OC claim surface, TOE gap surface, article boundary,
operator/K/domain/crown claims, and formal checks.

Voluntary progress closeout is forbidden while the SQLite queue has an open
row. If execution is interrupted by an outside runtime boundary, the response is
a forced handoff, not completion.

## Authority Stack

Every resumed session must read, in this order:

1. this order file;
2. `oc_continuous_completion_028.sqlite`, table `control`;
3. `OC_CONTINUOUS_COMPLETION_NOTEBOOK_028.md`;
4. `OC_ARTICLE_INTEGRATION_LEDGER_028.md`;
5. the referenced 027 Lean/Python artifacts.

If `control.open_total > 0`, the next action is the active row in
`queue_rows`. If `control.open_total = 0`, the current 028 queue is terminal,
but newly discovered OC source obligations must be appended before any broader
publication closeout.

## Terminal Statuses

The only terminal statuses allowed in 028 are:

- `PROVED_UNIVERSAL_METAMODEL_THEOREM`
- `REFUTED_OLD_FORMULATION`
- `DOMAIN_INSTANTIATION_REQUIRED`
- `REPAIRED_AND_BOUND_TO_OCU`

`DOMAIN_INSTANTIATION_REQUIRED` is terminal only for the metamodel question. It
means OC-U proved the correct boundary: a domain claim cannot be promoted from
core vocabulary alone and needs local observables, constraints, operators,
invariants, falsifiers, and evidence bindings.

## Scientific Basis

027 proves the repaired OC-U core over articulated descriptions:

- absorption by an existing same-description repair;
- extension/refinement/new axis when no same repair exists but extension repair
  exists;
- collapse when repair and extension fail and invariant failure is declared;
- underdetermination when evidence binding or invariant failure is missing;
- projection non-inflation;
- role-preserving operator injectivity;
- K-ladder reference step and non-uniqueness of K-count.

025 and 026 remain guard layers. They prevent old overclaims from re-entering
the article:

- the old two-outcome fork is incomplete;
- global operator minimality is false;
- unique/exhaustive K0-K12 wording is false;
- unconditional law wording is false;
- core-only domain validation is false;
- crown stones are not core theorems unless separately derived.

## Required Integration Obligations

028 must cover:

1. source-root OC central principle wording;
2. universal language/metamodel wording;
3. old two-outcome fork as a refuted formulation;
4. operator catalogue;
5. K-level ladder and count;
6. law-like statements;
7. crown-stone claims;
8. domain projections;
9. TOE `FAIL_CLOSED` metamodel gaps;
10. article boundary and forbidden wording;
11. formal Lean/Lake and Python witness checks.

## Closeout Gate

028 closeout is allowed only when:

- all queue rows have one of the four allowed terminal statuses;
- every terminal row has an artifact reference and proof or boundary body;
- the article ledger contains no allowed wording that reintroduces the old
  two-outcome event law, unique cosmic K-count, global operator minimality, or
  core-only domain validation;
- `lake build` passes for the 027 Lean project;
- 027 Python witnesses pass;
- the 028 validator passes.
