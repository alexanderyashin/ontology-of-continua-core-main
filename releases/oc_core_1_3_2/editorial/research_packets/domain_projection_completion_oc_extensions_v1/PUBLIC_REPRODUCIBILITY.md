# Reproducibility

Run from the governed Core working tree:

```powershell
python logion/k7/spe/orchestrator/research/run_domain_projection_completion_v1.py --execute-replays on --stage-public on
```

The packet is deterministic with respect to the K atlas, domain registry, nonclaim ledger, portfolio registry, replay reports, and script version.
