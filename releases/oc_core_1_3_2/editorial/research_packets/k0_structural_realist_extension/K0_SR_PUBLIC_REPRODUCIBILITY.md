# Public Reproducibility Summary

This packet exports deterministic toy-model summaries only. The run seed was
`1302`, with three runners and zero failures.

Support ceiling: `SIMULATION_ILLUSTRATION_ONLY`.

Validation claim allowed: `false`.

## Toy Runs

- Distinction network toy model: stable edge count `20`, emergent partition
  count `4`, boundary crossed `true`.
- Symmetry-breaking axis-generation toy model: axis selected `true`, emergent
  axis label `A_order_parameter`, stable omega region `true`.
- Contextual-semantics toy model: context count `3`, local solution total `6`,
  global solution total `0`, gluing obstruction `true`.

## Reuse Boundary

The reusable pipeline can rerun intake, role artifacts, trace checks, source
manifest generation, red-team review, decision memo generation, and public packet
export for later hypotheses. This packet is the product of one case run; the
research machine and release machine remain separate from this case.

## Public Limits

The toy models are diagrams of compatibility and failure modes. They do not
promote K0, do not widen the OC claim ledger, and do not authorize publication
without owner approval.
