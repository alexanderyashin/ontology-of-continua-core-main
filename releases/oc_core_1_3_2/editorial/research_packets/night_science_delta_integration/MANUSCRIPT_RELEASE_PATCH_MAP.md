# Manuscript/Release Patch Map

All rows are governed patch plans. They do not promote canonical claims.

| Group | Manuscript Action | Release Action |
| --- | --- | --- |
| `claim_demotions` | Patch claims and captions so demoted/rejected Codex rows cannot enter canonical/core prose. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
| `domain_replay` | Keep prediction wording structural unless protocol/code/data/hash/falsifier exist. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
| `general_governed_integration` | Route residual items into governed proof/release backlog. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
| `release_hygiene` | Synchronize release notes with claim ceilings and no-send packet state. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
| `source_replay` | Add source-replay checklist appendix and block manuscript promotion until source replay passes. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
| `theorem_fate_proof_dag` | Demote unsupported theorem labels to conjecture/proof-obligation rows unless bounded proof support exists. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
| `tsukrov_regression` | Preserve Tsukrov closure references and reviewer acknowledgement without endorsement. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
| `tuple_minimality_k_rigor` | Add tuple component-role, ablation, criteria, falsifier, and reduction-test tables; no unsupported minimality theorem. | Expose this group as a no-send release support packet and keep implementation as a governed patch plan. |
