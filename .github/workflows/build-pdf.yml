name: Build LaTeX PDF

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install TeX Live
        run: |
          sudo apt-get update
          sudo apt-get install -y texlive-full latexmk

      - name: Build PDF
        run: |
          mkdir -p build
          latexmk -pdf main.tex -interaction=nonstopmode -output-directory=build
          # Optional: copy log to logs directory if you want it tracked
          mkdir -p build/logs
          cp build/main.log build/logs/compile.log || true

      - name: Upload PDF Artifact
        uses: actions/upload-artifact@v4
        with:
          name: OK-Core-1.1-PDF
          path: build/main.pdf
