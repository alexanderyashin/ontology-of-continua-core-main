# OC Core Release Package Cascade

Status: `OC_CORE_RELEASE_PACKAGE_CASCADE_READY`
Artifact hash: `4b1f05445e4484eece901d954c01f3a7c7abd61d56e5a2b06cd4b43ca63bf52f`

## L0 Standard Artifact Set

### 1. Release guide `release_guide`

Purpose: Orient the reader and declare reading order, scope, boundaries, and package navigation.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Orient the reader and declare reading order, scope, boundaries, and package navigation.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 2. Master monograph `master_monograph`

Purpose: Carry the complete scientific argument in one coherent monograph structure.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Carry the complete scientific argument in one coherent monograph structure.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 3. Journal core article `journal_core_article`

Purpose: Provide an article-scale scientific entry point for external review.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Provide an article-scale scientific entry point for external review.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 4. Methods and reproducibility companion `methods_repro_companion`

Purpose: Expose methods, replay, data, software, and traceability obligations.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Expose methods, replay, data, software, and traceability obligations.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 5. Reviewer attack and response map `reviewer_attack_response_map`

Purpose: Collect adversarial objections, claim boundaries, closure evidence, and remaining caveats.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Collect adversarial objections, claim boundaries, closure evidence, and remaining caveats.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 6. Public evidence bundle `public_evidence_bundle`

Purpose: Package source evidence, proof/data ledgers, replay material, and review summaries.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Package source evidence, proof/data ledgers, replay material, and review summaries.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 7. Checksums and manifest `integrity_manifest`

Purpose: Bind files, hashes, roles, and provenance without becoming the reader-facing argument.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Bind files, hashes, roles, and provenance without becoming the reader-facing argument.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 8. Citation and metadata `citation_metadata`

Purpose: Provide citation, authorship, license, identifiers, and machine-readable metadata.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: Provide citation, authorship, license, identifiers, and machine-readable metadata.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.

### 9. Release notes and changelog `release_notes_changelog`

Purpose: State what changed, why it matters, and how to use the release safely.

Why included: Required to make an OC Core release scientifically readable, reproducible, citable, and reviewable.

Mini-cascade:
- L1 `purpose_audience_reader_contract`: State what changed, why it matters, and how to use the release safely.
- L2 `required_sections`: Declare mandatory sections before writing or packaging.
- L3 `source_mapping_dependencies`: Bind each section to aggregator, release instance, mapping, and evidence sources.
- L4 `transformation_rules`: Transform sources into readable scientific text or package inventory using text-fill rules.
- L5 `validation_gates`: Block missing purpose, missing trace, weak claim boundary, broken links, bad encoding, or stale version.
- L6 `public_surface_constraints`: Keep public-facing material readable and free of control-plane or raw-ledger presentation.
