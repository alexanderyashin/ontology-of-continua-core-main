{
  "role": "reproducibility_auditor",
  "findings": [
    {
      "severity": "MEDIUM",
      "artifact_ref": "reports/OC_CORE_1_3_3_POST_GENERATION_REPRODUCIBILITY_MANIFEST.json",
      "claim": "post-generation reproducibility verdict PASS binds the current release-critical manifest state",
      "failure_mode": "The manifest is not itself in COMPARE_REFS, and stable_payload_sha256 excludes schema_id, stable_payload_sha256, previous_manifest_self_check, and verdict. Exact manifest bytes and the PASS wrapper are therefore not byte-compared or hash-bound by the same replay evidence.",
      "required_repair": "Add a non-cyclic outer attestation or two-pass verifier that binds the exact reproducibility manifest bytes, including verdict, and fails when the regenerated wrapper differs.",
      "status": "OPEN"
    },
    {
      "severity": "MEDIUM",
      "artifact_ref": "tools/verify_oc133_reproducible_temp_tree.py",
      "claim": "clean temp-tree replay is free of hidden local state",
      "failure_mode": "Producer commands inherit sys.executable and the ambient process environment; the manifest records only executed_with_current_python and explicitly limits cross-environment byte identity. PASS proves same ambient stack replay, not hidden-state-free clean-checkout reproducibility.",
      "required_repair": "Run producers under a scrubbed deterministic environment and record Python, zlib, locale, timezone, SOURCE_DATE_EPOCH, and relevant env inputs, or keep all public claims scoped to same-host ambient-stack replay.",
      "status": "OPEN"
    },
    {
      "severity": "LOW",
      "artifact_ref": "simulations/run_all.py",
      "claim": "simulation replay covers 11 distinct k-level phenomena",
      "failure_mode": "The expected simulation manifest drives 11 lanes with the same seed and identical expected trace length, stability score, and boundary result; the certificate source manifest also shows the same runner hash across k-level runners. This is reproducible but weak as phenomenon coverage if promoted beyond illustration.",
      "required_repair": "Keep the support ceiling as SIMULATION_ILLUSTRATION_ONLY everywhere, or replace template-equivalent lanes with differentiated phenomenon-specific models and expected outputs.",
      "status": "OPEN"
    }
  ]
}