{
  "role": "theorem_theater_auditor",
  "findings": [
    {
      "severity": "CRITICAL",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-MIN - Within the declared OC verdict class, each promoted tuple component is required by a verdict-changing witness pair.",
      "failure_mode": "The Lean theorem proves a toy verdict class where `componentVerdict` passes exactly when `present=true`, then constructs `keep` and `drop` by flipping that same Boolean for any component. This is circular and does not prove minimality of the promoted OC tuple under the declared OC verdict class. The finite runner mirrors the same weakness by checking presence flags and `changed_fields`, not executing real OC verdict semantics.",
      "required_repair": "Formalize the actual declared OC verdict function and component-removal maps; prove one real keep/drop witness per promoted component against that function; make the finite runner compute those verdicts instead of accepting self-declared flags. Demote T133-MIN until this exists.",
      "status": "OPEN"
    },
    {
      "severity": "CRITICAL",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-KLEVEL - Every adjacent K-level transition K0->K12 has a witness, a reduction-failure criterion, and a lawful demotion criterion.",
      "failure_mode": "`every_adjacent_transition_has_witness` uses one generic constructor for every `AdjacentK`; `retainedTransitionEvidence` simply sets `witnessRetained=true`, `addedAxisObservable=true`, and `reduction=losesWitness`. The transition-specific added axes, witness pairs, reduction-failure criteria, and demotion criteria are not formalized. The finite runner only checks booleans and `transition_count=12`, so the promoted K-level irreducibility claim is stronger than the evidence.",
      "required_repair": "Formalize each K0->K1 through K11->K12 transition with its own witness pair, reduction relation, and lawful demotion condition; prove the twelve cases individually and make the runner validate transition-specific predicates. Demote T133-KLEVEL until then.",
      "status": "OPEN"
    },
    {
      "severity": "CRITICAL",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-K-ZERO - Continuumness zero follows from a declared zero-cause family, not only from empty admissibility or empty cycles.",
      "failure_mode": "`kZeroLicensed` is defined as `hasZeroCause`, and `k_zero_iff_declared_zero_cause` is `Iff.rfl`. The Lean file does not model a continuumness score, admissibility, cycles, empty/nonempty cases, or the advertised distinction from the old empty-set biconditional. The finite model accepts `k_value=0` plus any zero-cause Boolean instead of computing k.",
      "required_repair": "Define the continuumness functional, admissibility and cycle structures, zero-cause semantics, and nonempty counterexamples; prove zero-cause licensing as a substantive theorem and make the finite runner compute k from the model. Demote T133-K-ZERO until this is done.",
      "status": "OPEN"
    },
    {
      "severity": "HIGH",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-OMEGA-STATUS - Live status, death, residue, and rebirth are distinct typed predicates and morphism classes.",
      "failure_mode": "`declared_death_blocks_live` proves a contradiction only after assuming both `L.live x=false` and `L.live x=true`; the death predicate itself does no work. The file does not prove death excludes live, nor does it prove the advertised residue/rebirth/status non-interchangeability beyond separate fields and enums.",
      "required_repair": "Add lifecycle consistency invariants such as death -> not live, typed residue and rebirth existence/compatibility rules, and disjointness theorems for the promoted status and morphism classes; update finite negatives to test those invariants rather than hard-coded Boolean conflicts.",
      "status": "OPEN"
    },
    {
      "severity": "HIGH",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-CYCLE - Live status requires an explicit cycle mode or non-vacuous maintenance predicate.",
      "failure_mode": "`eligibleLive` includes `supportWitnessed` by definition, and `eligible_live_requires_cycle_or_maintenance` merely projects that conjunct. Raw `R.live x=true` can still hold with no cycle or maintenance support, and non-vacuous maintenance is not formalized.",
      "required_repair": "Either define public live status as `eligibleLive` or add a theorem-level invariant `live -> supportWitnessed`; formalize non-vacuous maintenance with an executable support obligation and add finite cases that reject vacuous maintenance.",
      "status": "OPEN"
    },
    {
      "severity": "HIGH",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-ID - Residue preservation and rebirth are not identity continuation unless declared identity invariants are preserved.",
      "failure_mode": "`restartClass` is defined as identity iff `identityInvariant` is true and rebirth otherwise; `invariant_lost_blocks_identity` is just unfolding that definition. Residue preservation, rebirth construction, identity-invariant preservation, and composition are not modeled, so the promoted morphism-class theorem is mostly definitional.",
      "required_repair": "Formalize residue morphisms, rebirth morphisms, identity morphisms, invariant preservation, and their composition laws; prove disjointness and the no-identity-continuation result from those structures rather than from an if-then classifier.",
      "status": "OPEN"
    },
    {
      "severity": "HIGH",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-DIM - Historical axis activation may be monotone while effective working rank decreases.",
      "failure_mode": "`historical_axis_survives_rank_drop` only exhibits one record with `historical=2` and `effective=1`. There is no time-indexed activation history, monotonicity property, transition, effective working-rank decrease, or demotion criterion in the formal evidence.",
      "required_repair": "Formalize time-indexed historical axes and effective rank, prove historical monotonicity plus a genuine rank-drop transition, and add finite witnesses that compute both quantities across states.",
      "status": "OPEN"
    },
    {
      "severity": "HIGH",
      "artifact_ref": "proofs/finite_model_checks/run_finite_model_checks.py",
      "claim": "T133-BOUNDARY - Metric thresholds are a specialization of typed classifier boundaries.",
      "failure_mode": "The boundary evaluator accepts `metric_specialization` whether it is true or false (`in {True, False}`), so a case can pass without the metric-specialization condition. The Lean evidence is also only an `rfl` embedding of a Nat threshold into a classifier, not the advertised typed boundary representation with measurement-rule safeguards.",
      "required_repair": "Require an actual metric-specialization and measurement rule for metric-threshold cases, add a negative case for metric-without-measure, and formalize the classifier embedding theorem beyond definitional equality.",
      "status": "OPEN"
    },
    {
      "severity": "HIGH",
      "artifact_ref": "formal/lean/OC133V12.lean",
      "claim": "T133-HYBRID - Differential operator notation is a smooth-realization specialization of typed update semantics.",
      "failure_mode": "`differential_notation_requires_chart` simply returns the `derivative_requires_chart` field assumed inside `SmoothSystem`, and `smooth_operator_is_update_special_case` is projection by `rfl`. This does not prove that differential notation is a smooth-realization specialization of typed update semantics or that non-smooth proof/rewrite domains are blocked.",
      "required_repair": "Formalize typed update semantics, smooth realization maps, chart witnesses, derivative availability, and non-smooth rejection; prove the specialization and blocking results from those definitions rather than assuming them as fields.",
      "status": "OPEN"
    },
    {
      "severity": "HIGH",
      "artifact_ref": "review/OC_1_3_3_TOTAL_ATTACK_MATRIX.json",
      "claim": "All critical/high theorem-theater attacks are closed (`critical_unresolved_total=0`, `high_unresolved_total=0`).",
      "failure_mode": "The attack matrix closes critical proof objections with weak tokens such as `semantic_evaluator=true` for T133-MIN and theorem-name references for T133-KLEVEL, even though the inspected Lean and finite evidence are definitional or self-confirming. The unresolved totals therefore mask current release-blocking theorem-theater defects.",
      "required_repair": "Reopen every closure whose cited theorem or finite check is only definitional, circular, or weaker than the promoted claim; require each closure row to cite a substantive formal theorem or executable semantic witness that directly covers the attacked claim.",
      "status": "OPEN"
    },
    {
      "severity": "MEDIUM",
      "artifact_ref": "docs/OC_1_3_3_PHENOMENON_COVERAGE_MATRIX.json",
      "claim": "OC133-NOSEND-001 - Publication and journal submission remain locked until separate owner approval.",
      "failure_mode": "The no-send coverage row routes evidence through `formal/lean/OC133V12.lean` and says `claim -> Lean theorem/proof sheet -> semantic finite case`, but the inspected Lean file contains no no-send theorem and the theorem inventory has no no-send proof sheet. The manifest lock itself is present, so this is an evidence-route overclaim rather than an observed send violation.",
      "required_repair": "Remove the Lean/proof-sheet route from the no-send coverage row or add a named no-send theorem, inventory entry, and proof sheet; keep the manifest fields as the load-bearing no-send evidence until then.",
      "status": "OPEN"
    }
  ]
}